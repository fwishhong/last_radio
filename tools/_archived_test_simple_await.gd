extends SceneTree
# ARCHIVED — scratch test from a debugging session. Hangs on missing scene
# reference. No production role.

func _initialize() -> void:
	print("SKIP: _archived_test_simple_await.gd")
	quit(0)